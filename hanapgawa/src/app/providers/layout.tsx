import type { Metadata } from "next";
export const metadata: Metadata = { title: "Mga Service Provider — HanapGawa" };
export default function ProvidersLayout({ children }: { children: React.ReactNode }) { return children; }
