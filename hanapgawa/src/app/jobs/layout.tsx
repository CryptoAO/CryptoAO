import type { Metadata } from "next";
export const metadata: Metadata = { title: "Hanap Trabaho — HanapGawa" };
export default function JobsLayout({ children }: { children: React.ReactNode }) { return children; }
